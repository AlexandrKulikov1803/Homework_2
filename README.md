# Проект Homework_2

## Описание

## Установка

Клонируйте репозиторий:

```
git clone git@github.com:AlexandrKulikov1803/Course_work_1.git
```

## Использование

### Модуль base_product.py

Класс ***BaseProduct*** является абстрактным классом, в котором реализованы абстрактные методы: ***new_product***
(класс-метод), ***__str__***, ***__add__***, геттер и сеттер ***price***.
Применение этих методов будет описано при добавлении класса-наследника Product.

### Модуль print_mixin.py

Класс ***PrintMixin*** добавляет в другие классы отображение информации при отладке с помощью магического метода
***__repr__***.

Формат отображения:

```
{Название класса}('{Имя продукта}', '{Описание продукта}', {Цена продукта}, {Количество продуктов})
```

Например:

```
Product('Iphone 15', '512GB, Gray space', 210000.0, 8)
Smartphone('Iphone 14', '256GB, Gray space', 210000.0, 8)
LawnGrass('Газонная трава', 'Элитная трава для газона', 500.0, 20)
```

Данный метод вызывается при инициализации ***PrintMixin*** через магический метод ***__init__***.

### Модуль product.py

Класс ***Product*** содержит основную информацию о продукте:

+ Название продукта (name: str)
+ Описание (description: str)
+ Стоимость (__price: float)
+ Количество (quantity: int)

А также список всех созданных продуктов (all_products: list).

В данном классе реализованы магические методы ***__str__*** и ***__add__***.

Метод ***__str__*** отображает информацию об объекте класса для пользователей в формате:

```
{Название продукта}, {стоимость} руб. Остаток: {количество} шт.
```

Например:

```
Samsung Galaxy S23 Ultra, 180000.0 руб. Остаток: 5 шт.

```

Метод ***__add__*** позволяет высчитать полную стоимость товаров на складе одного класса:

```
# Вызов метода
product_1 = Product("Samsung Galaxy S23 Ultra", "256GB, Серый цвет, 200MP камера", 180000.0, 5,)
product_2 = Product("Iphone 15", "512GB, Gray space", 210000.0, 8)
print(product_1 + product_2)

# Выходные данные
2580000.0
```

В классе имеется возможность добавлять новый продукт с помощью класс-метода ***new_product***. На вход подаётся словарь
с параметрами нового продукта. При этом поведение данного метода зависит от входных данных.

Пример №1.
Если передан новый продукт, которого ещё нет на складе, то метод возвращает новый продукт.

```
# Вызов метода
product_1 = Product("Samsung Galaxy S23 Ultra", "256GB, Серый цвет, 200MP камера", 180000.0, 5)

parameters = {"name": "Iphone 15", "description": "512GB, Gray space", "price": 210000.0, "quantity": 8}
new_product = Product.new_product(parameters)

print(new_product)

# Выходные данные
Iphone 15, 210000.0 руб. Остаток: 8 шт.
```

Пример №2.
Если передан продукт, который уже лежит на складе и при этом стоимость указана прежняя стоимость или меньше, то метод
выводит сообщение о том, что такой продукт уже имеется в наличии, а затем добавляет к количеству имеющегося товара
количество нового и возвращает этот продукт со старой ценой.

```
# Вызов метода
product_1 = Product("Samsung Galaxy S23 Ultra", "256GB, Серый цвет, 200MP камера", 180000.0, 5)

parameters = {"name": "Samsung Galaxy S23 Ultra", "description": "256GB, Серый цвет, 200MP камера", "price": 180000.0, "quantity": 5}

new_product = Product.new_product(parameters)
print(new_product)

# Выходные данные
При попытке добавления нового товара Samsung Galaxy S23 Ultra обнаружен аналогичный товар. Количество товаров в указанной категории было увеличено. Новая позиция не добавлена.
Samsung Galaxy S23 Ultra, 180000.0 руб. Остаток: 13 шт.
```

Пример №3.
Если передан продукт, который уже лежит на складе и при этом стоимость указана выше прежней, то метод выводит сообщение
о том, что такой продукт уже имеется в наличии, а затем добавляет к количеству имеющегося товара количество нового и
возвращает этот продукт с новой ценой.

```
# Вызов метода
product_1 = Product("Samsung Galaxy S23 Ultra", "256GB, Серый цвет, 200MP камера", 180000.0, 5, )

parameters = {"name": "Samsung Galaxy S23 Ultra", "description": "256GB, Серый цвет, 200MP камера", "price": 2100000.0, "quantity": 8}
new_product = Product.new_product(parameters)
print(new_product)

# Выходные данные
При попытке добавления нового товара Samsung Galaxy S23 Ultra обнаружен аналогичный товар. Количество товаров в указанной категории было увеличено. Новая позиция не добавлена.
Samsung Galaxy S23 Ultra, 2100000.0 руб. Остаток: 13 шт.
```

Кроме указанных выше методов имеются геттер и сеттер для аттрибута ***price(private)***.

+ геттер возвращает стоимость товара
+ сеттер позволяет изменить его стоимость
    + если новая цена равна или меньше нуля, то выводится сообщение: "Цена не должна быть нулевая или отрицательная"
    + если цена положительная и меньше старой цены, то программа предупреждает, что цена станет меньше и просит дать
      согласие на изменение цены
    + если цена положительная и равна или больше старой цены, то стоимость товара меняется автоматически

### Модуль lawn_grass.py

Класс ***LawnGrass*** содержит основную информацию о продукте - газонная трава:

+ Название продукта (name: str)
+ Описание (description: str)
+ Стоимость (__price: float)
+ Количество (quantity: int)
+ Страна-производитель (country: str)
+ Срок прорастания (germination_period: str)
+ Цвет (color: str)

Класс ***LawnGrass*** является дочерним классом ***Product***, поэтому он может использовать все методы, что и
родительский класс. Например, магический метод ***__add__***:

```
# Вызов метода
grass1 = LawnGrass("Газонная трава", "Элитная трава для газона", 500.0, 20, "Россия", "7 дней", "Зеленый")
grass2 = LawnGrass("Газонная трава 2", "Выносливая трава", 450.0, 15, "США", "5 дней", "Темно-зеленый")

grass_sum = grass1 + grass2
print(grass_sum)

# Выходные данные
16750.0
```

### Модуль smartphone.py

Класс ***Smartphone*** содержит основную информацию о продукте - смартфон:

+ Название продукта (name: str)
+ Описание (description: str)
+ Стоимость (__price: float)
+ Количество (quantity: int)
+ Производительность (efficiency: str)
+ Модель (model: str)
+ Объем встроенной памяти (memory: int)
+ Цвет (color: str)

Класс ***Smartphone*** является дочерним классом ***Product***, поэтому он может использовать все методы, что и
родительский класс. Например, магический метод ***__add__***:

```
# Вызов метода
smartphone1 = Smartphone("Samsung Galaxy S23 Ultra", "256GB, Серый цвет, 200MP камера", 180000.0, 5, 95.5, 
                         "S23 Ultra", 256, "Серый")
smartphone2 = Smartphone("Iphone 15", "512GB, Gray space", 210000.0, 8, 98.2, "15", 512, "Gray space")

smartphone_sum = smartphone1 + smartphone2
print(smartphone_sum)

# Выходные данные
2580000.0
```

### Модуль category.py

Класс ***Category*** содержит основную информацию о категории:

+ Название категории (name: str)
+ Описание (description: str)
+ Список продуктов в данной категории (__products: list)
+ Общее количество всех продуктов в данной категории (total_quantity: int)

А также имеются переменные для учёта количества категорий (category_count: int) и количества продуктов (product_count:
int).

В данном классе реализован магический метод ***__str__***, высчитывающий общее количество продуктов в категории и
отображающий информацию об объекте класса для пользователей в формате:

```
{Название категории}, количество продуктов: {общее количество продуктов} шт."
{Название продукта}, {стоимость} руб. Остаток: {количество} шт.
```

Например:

```
Смартфоны, количество продуктов: 27 шт.
```

Метод ***add_product*** позволяет добавить продукт в категорию, ничего не возвращает. Продукт должен быть экземпляром
класса ***Product*** или дочерних от него классов (***Smartphone*** или ***LawnGrass***).

Пример:

```
product1 = Product("Samsung Galaxy S23 Ultra", "256GB, Серый цвет, 200MP камера", 180000.0, 5)
product2 = Product("Iphone 15", "512GB, Gray space", 210000.0, 8)

category1 = Category(
    "Смартфоны",
    "Смартфоны, как средство не только коммуникации, но и получения дополнительных функций для удобства жизни",
    [product1, product2]
)

product3 = Smartphone("Samsung Galaxy S23 Ultra", "256GB, Серый цвет, 200MP камера", 180000.0,
                      5, 95.5, "S23 Ultra", 256, "Серый")
category1.add_product(product3)
```

Кроме указанных выше методов имеются два геттера для аттрибута ***products(private)***.

+ первый геттер возвращает список экземпляров класса Product
+ второй геттер возвращает строку с информацией о товарах с использованием магического метода str

Например:

```
Samsung Galaxy S23 Ultra, 180000.0 руб. Остаток: 5 шт.
Iphone 15, 210000.0 руб. Остаток: 8 шт.
```

### Модуль product_iterator

В классе ***ProductIterator*** реализован итератор, который будет пробегать по списку продуктов переданной ему
категории.

Для этого прописаны два магических метода: ***iter*** и ***next***.

+ Первый возвращает итератор
+ Второй возвращает следующий элемент из списка продуктов

### Модуль utils.py

Модуль ***utils.py*** содержит две функции: ***read_json***, ***create_objects_from_json***.

Функция ***read_json*** считывает данные json-файла и преобразует их в список словарей.

```
# вызов функции
print(read_json("путь к файлу/файл.xlsx"))

# выход функции
[{'name': 'Смартфоны', 'description': 'Смартфоны, как средство не только коммуникации, но и получение дополнительных функций для удобства жизни', 'products': [{'name': 'Samsung Galaxy C23 Ultra', 'description': '256GB, Серый цвет, 200MP камера', 'price': 180000.0, 'quantity': 5}, {'name': 'Iphone 15', 'description': '512GB, Gray space', 'price': 210000.0, 'quantity': 8}, {'name': 'Xiaomi Redmi Note 11', 'description': '1024GB, Синий', 'price': 31000.0, 'quantity': 14}]}, {'name': 'Телевизоры', 'description': 'Современный телевизор, который позволяет наслаждаться просмотром, станет вашим другом и помощником', 'products': [{'name': '55" QLED 4K', 'description': 'Фоновая подсветка', 'price': 123000.0, 'quantity': 7}]}]
```

+ Тестирование считывания данных json-файла и преобразования в список словарей.
+ Проверка работы функции при получении пути к несуществующему файлу.
+ Проверка работы функции при получении пути к файлу с некорректными данными.

Функция ***create_objects_from_json*** пробегает по списку словарей и создаёт список экземпляров классов и подклассов
внутри них.

```
# вызов функции
categories_data = create_objects_from_json(список словарей)
print(categories_data)
print(categories_data[0].name)
print(categories_data[0].description)
print(categories_data[0].products)
print(categories_data[0].products[0].name)


# выход функции
[<src.classes.Category object at 0x0000015E97786900>, <src.classes.Category object at 0x0000015E97725090>]
Смартфоны
Смартфоны, как средство не только коммуникации, но и получение дополнительных функций для удобства жизни
[<src.classes.Product object at 0x0000020FF4B367B0>, <src.classes.Product object at 0x0000020FF4AD7750>, <src.classes.Product object at 0x0000020FF4AD79D0>]
Samsung Galaxy C23 Ultra
```

+ Тестирование преобразования списка словарей в список экземпляров класса.
